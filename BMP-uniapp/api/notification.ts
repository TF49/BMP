/**
 * 用户端通知 API（仅导出会员页使用的符号；实现见 @/api/internal/notification）
 */
export type { NotificationItem } from '@/api/internal/notification'
export { getNotificationList } from '@/api/internal/notification'
