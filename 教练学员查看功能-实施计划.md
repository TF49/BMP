# 教练学员查看功能 · 实施计划

## P0 冻结覆盖说明

本节为最终冻结决策，优先级高于本文后续早期草案中的冲突描述：

- 首版交付 P1—P5，首版 P1—P5 不实现 WebSocket；P6 为独立增强，不阻塞上线。
- 教练—学员关系由有效课程预约推导，历史权限保留至最后一次有效课程结束后 180 天。
- 列表只返回 `maskedPhone`；详情完成关系鉴权后才返回完整 `phone`；身份证只返回 `idCardTail`。
- 不返回余额、充值额、充值记录或其他课程消费；只返回可关联到当前教练课程预约的 COURSE 净消费。
- 考勤独立于预约状态：0未登记、1已签到、2已完成、3缺席；不得自动把未签到推断为缺席。
- 旧 `status=4` 只迁移为 `attendance_status=2`，取消记录不得迁移为缺席。
- `/for-coach/status` 只允许课程开始前取消；签到、完成、缺席统一走考勤 action 命令接口。
- 自动结束时考勤1转2、考勤3保持3、考勤0保持0。
- 越权异常统一返回403，Controller 不得捕获 AccessDeniedException 并转换为普通业务响应。
- 重查询超时：有最后成功缓存时返回旧缓存；无缓存时返回503和 traceId，禁止伪造空分页。
- 头像统一复用 `/static/placeholders/avatar.svg`；教练底栏保持4项，工作台增加全宽“我的学员”入口。

---

## 实施进度总览

> **进度更新日期：2026-07-14**
> **首版 P1–P5 完成度：约 97%（功能代码已完成，剩 DBA/生产迁移确认与真机验收）**
> **完整 P1–P6 完成度：约 84%（P6 WebSocket、双账号 E2E 及部分契约/迁移测试未落地）**
> **交付范围：BMP-uniapp 教练端 + Java 后端（Vue Web 管理端教练模块未接入本功能）**

| 阶段 | 状态 | 完成度 | 说明 |
|------|------|--------|------|
| P0 设计冻结 | ✅ 代码已对齐 | — | 文档评审项仍待签字，实现已按冻结决策落地 |
| P1 数据层 | ✅ 已完成 | ~95% | DDL、考勤字段、写入链路到位；聚合 SQL 迁至独立 `CoachStudentMapper` |
| P2 后端接口 | ✅ 已完成 | ~95% | 6 个 REST 端点、权限、限流、降级缓存、隐私裁剪均已实现 |
| P3 前端 API | ✅ 已完成 | 100% | `coachSelf.ts` + `types/coachStudents.ts` + API 契约测试 |
| P4 路由入口 | ✅ 已完成 | ~95% | 工作台卡片、页面注册、签到页跳转均已接入 |
| P5 前端 UI | ✅ 代码已完成 | 100% | 提交 `0f454b7` 补齐深链、状态分类、月度出勤率、趋势跳转、路由预填与拨号规范 |
| P6 WS + 全量回归 | ❌ 核心未开始 | ~20% | 已有主体单测；WebSocket、双账号 E2E 及 Mapper/DDL 等契约测试未做 |

**主链路代码与自动化测试已通：** 教练工作台 →「我的学员」→ 列表筛选/排序 → 学员详情五 Tab（包含消费记录 Tab） → 课程安排直达预约详情 → 单课签到页「查看学员详情 →」。真机 4 case 仍需上线前确认。

**剩余上线工作：**
1. DBA 审核 DDL/EXPLAIN，并确认生产迁移与 `<500ms` 指标
2. 微信开发者工具/真机回归：有学员、空态、风险学员、到期学员
3. P6 WebSocket 三类事件 + 双账号 E2E 回归

---

> 编制日期：2026-07-08 · 进度更新：2026-07-14
> 适用项目：BMP（羽毛球 / 体育场馆综合管理平台）
> 文档状态：**实施中 · P1–P5 功能代码已完成，上线验收 + P6 待排期**
> 依据：前一轮《教练学员查看功能缺失问题分析》的完整结论，以及项目中现有的
> `CoachService`、`CourseBookingMapper`、`CoachTabBar`、`students.vue`、`courses.vue`、
> `badminton.sql` 等代码/数据风格；**实际落地时新增** `CoachStudentMapper`、`CoachStudentController`、
> `coachStudents.ts` 等独立模块，详见附录 B。

---

## 0. 全局设计决策（P0 阶段 · ✅ 代码已对齐，文档评审签字待补）

> 以下 12 项决策均已体现在当前代码实现中。

| 决策项 | 选择 | 理由 / 备注 |
|--------|------|-------------|
| 教练 ↔ 学员关联 | **复用 `biz_course → biz_course_booking` 链路**，不新增硬关联表 | 现项目"教练带学员"的所有数据天然存在于课程预约记录；如后续支持场地预约教练代课，再补 `biz_booking_coach_rel`。 |
| 课程包 vs 单次课 | **先做"单次课 + 按课程名称聚合进度"的轻量方案（方案 A）**，为未来课程包留 DDL 占位 | 当前 `biz_course` 是"一节课 = 一个 courseId"粒度，直接做课程包会牵涉订单/预约/扣课时的全链路改造，风险过大。 |
| 学员头像来源 | **通过 `sys_member.user_id → sys_user.avatar` 关联**，`sys_member` 不扩展 avatar 字段 | 与项目现有教练头像展示方式一致；返回时用 `CASE WHEN u.avatar IS NOT NULL THEN u.avatar ELSE '默认常量' END`。 |
| 接口命名空间 | **新增 `/api/coach/students/*` 独立一组（COACH 专属）**，不共用 `/api/member/*` | 避免通用 member 接口权限粒度过粗导致越权；也便于以后做教练专属字段扩展。 |
| 越权策略 | **白名单制 + 缓存加速**：必须在 `biz_course_booking` 中存在当前教练的交集记录，否则 403；越权校验结果缓存5分钟 | 兼容"会员属于其他场馆、教练跨场馆代课"两种情况，比按 `venueId` 粗暴过滤更精确；缓存避免高频查询成为性能瓶颈。 |
| 现有 `students.vue` 语义 | **保留为“单课程签到/预约学员名单”页面**，不承载“我的学员”全局列表；仅在每条学员行增加「查看学员详情 →」跳新详情页 | 避免破坏已经跑通的签到链路；新全局列表使用 `student-list.vue`，详情使用 `student-detail.vue`，页面语义不冲突。 |
| 实时更新 | **onShow + refresher 兜底**，P6 再叠加 WebSocket；首版 P1~P5 不依赖 WebSocket 也能跑通 | 与现有 `courses.vue` 模式保持一致；出了线上问题能快速回退。 |
| 敏感数据策略 | 手机号**列表页脱敏展示、详情页"拨打确认弹窗"显示全号**，后端返回全量；身份证号只返末 4 位 | 平衡隐私保护与业务需求：列表页防止泄露、拨打前二次确认增强安全感；身份证对教练是低需求字段，优先最小化暴露。 |
| 工作台入口布局 | **在现有3个横向卡片下方新增全宽"我的学员"卡片**，不改现有布局为2×2网格 | 现有工作台已采用 `grid-template-columns: repeat(3, minmax(0, 1fr))` 的3列布局，改为2×2会破坏现有视觉平衡；新增全宽卡片风险更小。 |
| 课程预约状态 vs 考勤状态 | **保留现有 `biz_course_booking.status`：0已取消、1待支付、2已支付、3进行中、4已完成；新增轻量 `attendance_status` 表示考勤：0未签到、1已签到/进行中、2已完成、3缺席** | 当前项目没有“缺席”预约状态，禁止把 `status=4` 当缺席；缺席必须通过独立考勤字段表达，否则出勤率无法可靠统计。 |
| 出勤率计算规则 | **出勤率 = 出勤完成数 / (出勤完成数 + 缺席数)**；完成数取 `attendance_status=2`，缺席数取 `attendance_status=3`；历史无考勤字段的数据仅可把 `status=4` 回填为完成，不自动推断缺席 | 统一前后端口径，避免误用现有预约状态码；明确排除待支付/已支付/进行中/已取消等非考勤完成态。 |
| 风险学员阈值 | **出勤率 < 70% 或 最近14天未上课**，支持配置化（`application.yml`）；“最近上课”按 `biz_course.course_date + end_time` 中最近一次 `attendance_status=2` 或历史 `status=4` 计算 | 首版使用固定阈值快速上线；不能用 `cb.update_time` 代表上课时间，避免状态更新时间污染风险判断。 |

---

## 1. P1 · 后端数据层（Mapper / SQL · 含 DDL 增量脚本）

### 1.1 目标
在既有的 Mapper 内补齐教练视角的 5 类聚合查询，保证单条 SQL 直接出字段，避免 Service 层 N+1。

### 1.2 涉及文件

> **状态：✅ 已完成（2026-07-13）** — 原计划写在 `CourseBookingMapper` 的聚合 SQL 实际拆至独立 `CoachStudentMapper`，职责更清晰。

- **数据库结构**（已合并至全量初始化脚本）：
  - `src/main/resources/sql/badminton.sql` — 已包含考勤字段、查询索引与历史数据初始化
  - `src/main/resources/sql/explain-coach-students.sql` — 上线前 EXPLAIN 验证脚本
  - `src/main/java/com/badminton/bmp/modules/coach/mapper/CoachStudentMapper.java` — 教练学员全部聚合 SQL（@Select 内联，无 XML）
- **修改**（考勤写入链路，已完成）：
  - `src/main/java/com/badminton/bmp/modules/course/entity/CourseBooking.java` — 新增 `attendanceStatus` / `actualCheckinTime` / `actualFinishTime`
  - `src/main/java/com/badminton/bmp/modules/course/mapper/CourseBookingMapper.java` — 考勤条件更新 SQL
  - `src/main/java/com/badminton/bmp/modules/course/service/CourseBookingService.java`
  - `src/main/java/com/badminton/bmp/modules/course/service/impl/CourseBookingServiceImpl.java` — `updateAttendanceForCoach(CHECK_IN/COMPLETE/ABSENT)`
- **未改**（原计划，实际不需要）：
  - `MemberMapper.java` — 学员信息在 `CoachStudentMapper` 内联 JOIN，未扩展 MemberMapper

### 1.3 新增 SQL 清单（建议方法名与现项目保持动词开头）

| # | 方法名（建议） | 说明 | 关键字段 / 能力 |
|---|----------------|------|-----------------|
| 1 | `findDistinctStudentsByCoachId` | 教练名下**去重学员列表**（支持分页、keyword、memberType、风险筛选、orderBy） | member 基础资料 + 关联用户头像 + `firstLessonTime / lastLessonTime / totalBookings / attendedCount / absentCount / cancelledCount / attendanceRate / totalPaidAmount / scheduledCount` |
| 2 | `countDistinctStudentsByCoachId` | 配合 #1 的总数统计 | 与 #1 完全相同的 WHERE 条件 |
| 3 | `getStudentCoachRelationStats` | 学员 × 教练 交叉 KPI 聚合（用于详情页顶卡） | 跟我总共预约多少节、出勤多少节、缺席多少节、累计消费、首/末次上课 |
| 4 | `getStudentProgressByCourseNameForCoach` | 按"课程名称"聚合的训练进度（轻量方案 A） | 课程名 → 已完成节数 / 总预约节数 + 累计时长 |
| 5 | `findStudentScheduleForCoach` | 某学员与我有关的课程预约（未来 + 历史，支持区间、status、分页） | 复用 `findAllByCoachId` 的字段丰富度，仅加 `memberId` 条件 |
| 6 | `findStudentAttendanceTimelineForCoach` | 出勤时间线（最近 30 节 / 支持分页） | 日期、课程名、时长、预约状态、考勤状态（完成/缺席/未签到/进行中）、备注 |
| 7 | `isStudentTaughtByCoach` | 纯 EXISTS 查询（越权校验专用） | 返回 `1` 或空，避免每次跑大聚合 |

#### 1.3.1 实际落地方法名对照（`CoachStudentMapper`）

| 计划方法名 | 实际方法名 | 状态 |
|------------|------------|------|
| `findDistinctStudentsByCoachId` | `findStudents` | ✅ |
| `countDistinctStudentsByCoachId` | `countStudents` | ✅ |
| `getStudentCoachRelationStats` | `findStudentDetail`（内聚 KPI） | ✅ |
| `getStudentProgressByCourseNameForCoach` | `findProgress` | ✅ |
| `findStudentScheduleForCoach` | `findSchedule` + `countSchedule` | ✅ |
| `findStudentAttendanceTimelineForCoach` | `findAttendance` + `countAttendance` | ✅ |
| `isStudentTaughtByCoach` | `existsValidCoachStudentRelation` + `findCoachStudentRelationValidUntil` | ✅ |
| — | `summarizeStudents` / `countRiskStudents` | ✅ 列表 Hero 汇总（计划外增强） |
| — | `findConsumeRecordsForCoach` / `countConsumeRecordsForCoach` | ✅ 教练范围消费 |

### 1.4 DDL 结构说明（已合并至 `badminton.sql`）

```sql
-- ============================================================
-- 教练视角学员查询 · 新增考勤字段 + 索引
-- 依赖：badminton.sql 现有的 biz_course / biz_course_booking / sys_member / sys_user
-- 版本：v1.2（修正预约状态码、MySQL兼容性、上课时间口径）
-- ============================================================

-- MySQL 兼容性要求：
-- 1. 项目当前 SQL 为 MySQL 风格，不能直接使用 CREATE INDEX IF NOT EXISTS。
-- 2. 正式脚本必须先查 information_schema.columns / information_schema.statistics，
--    确认字段或索引不存在后再执行 ALTER TABLE / CREATE INDEX。
-- 3. 下方为目标 DDL 片段，不可原样无判断地重复执行。

-- 关键：当前 biz_course_booking.status 没有“缺席”含义；
-- 保留预约状态码：0已取消、1待支付、2已支付、3进行中、4已完成；
-- 新增独立考勤字段，避免把 status=4 误判为缺席。
ALTER TABLE biz_course_booking
    ADD COLUMN attendance_status TINYINT(1) NULL DEFAULT 0 COMMENT '考勤状态（0-未签到，1-已签到/进行中，2-已完成，3-缺席）' AFTER status,
    ADD COLUMN actual_checkin_time DATETIME NULL COMMENT '实际签到时间' AFTER attendance_status,
    ADD COLUMN actual_finish_time DATETIME NULL COMMENT '实际完成时间' AFTER actual_checkin_time;

-- 关键：教练想"看学员"需要通过 course → course_booking 反查
CREATE INDEX idx_cb_course_member_status
    ON biz_course_booking (course_id, member_id, status, attendance_status, del_flag);

-- 出勤率计算专用索引（按学员+考勤状态查询）
CREATE INDEX idx_cb_member_attendance
    ON biz_course_booking (member_id, attendance_status, status, del_flag, course_id);

-- 教练课程时间范围查询优化索引；注意字段名为 start_time/end_time，不是 course_start_time/course_end_time
CREATE INDEX idx_c_coach_date_range
    ON biz_course (coach_id, course_date, start_time, end_time, status, del_flag);

-- 教练课程基础索引；badminton.sql 已存在 idx_coach_date(coach_id, course_date)，
-- 若 DBA 评估 idx_c_coach_date_range 覆盖充分，可不重复创建此索引。
CREATE INDEX idx_c_coach_date
    ON biz_course (coach_id, course_date, start_time, del_flag);

-- 列表搜索：支持教练按姓名/手机号模糊搜其学员
CREATE INDEX idx_sm_name_include
    ON sys_member (member_name, del_flag);
CREATE INDEX idx_sm_phone_include
    ON sys_member (phone, del_flag);

-- 风险学员筛选按“课程日期+结束时间”判断，不使用 cb.update_time 代表上课时间。
CREATE INDEX idx_cb_course_attendance
    ON biz_course_booking (course_id, attendance_status, status, del_flag);

-- 越权校验加速索引（高频EXISTS查询优化）
CREATE INDEX idx_c_coach_id_only
    ON biz_course (coach_id, id, del_flag);

-- ============================================================
-- 数据回填脚本（首次部署必须执行，用于历史数据补全）
-- ============================================================

-- 历史数据：现有 status=4 表示“预约已完成”，可回填为 attendance_status=2。
-- 不能把取消记录自动推断为缺席；缺席只能由后续教练明确标记。
UPDATE biz_course_booking
SET attendance_status = 2,
    actual_finish_time = COALESCE(actual_finish_time, update_time)
WHERE del_flag = 0
  AND status = 4
  AND (attendance_status IS NULL OR attendance_status = 0);

-- 不在 sys_member 上回填 first_lesson_time / last_lesson_time：
-- 当前 sys_member 无这两个字段，列表/详情接口应通过聚合查询按需返回。
```

#### 1.4.1 DDL 执行与回滚要求
- **幂等字段判断**：执行 `ALTER TABLE` 前必须查询 `information_schema.columns`，确认
  `attendance_status / actual_checkin_time / actual_finish_time` 不存在；若任一字段已存在，脚本必须跳过对应字段。
- **幂等索引判断**：执行 `CREATE INDEX` 前必须查询 `information_schema.statistics`，确认索引名不存在；不得依赖 `CREATE INDEX IF NOT EXISTS`。
- **线上大表策略**：如 `biz_course_booking` 行数超过 10 万，DBA 需评估在线 DDL 能力；优先使用数据库原生 online DDL 或维护窗口执行，避免锁表影响下单/签到。
- **回填批处理**：历史 `status=4` 回填为 `attendance_status=2` 时按主键范围分批执行，每批建议 1000~5000 行；每批记录影响行数，失败后可从最后成功 id 继续。
- **回滚策略**：字段新增后不立即删除；若上线后需回退应用，保留新增字段和索引不影响旧代码。只有确认无代码依赖后，另发 DBA 审核脚本删除字段/索引。
- **验证 SQL**：
  ```sql
  SELECT status, attendance_status, COUNT(*)
  FROM biz_course_booking
  WHERE del_flag = 0
  GROUP BY status, attendance_status;
  ```
  验收标准：历史 `status=4` 的记录应主要落在 `attendance_status=2`；`status=0` 不得被批量回填为 `attendance_status=3`。

### 1.5 编码硬性约束
- 所有新 SQL 的 coachId 条件，统一使用
  `INNER JOIN biz_course c ON cb.course_id = c.id AND c.coach_id = ?`
  （与现有 `findAllByCoachId` 写法一致），禁止改从 `venue_id` 间接推断。
- `attendanceRate` 计算必须使用 `NULLIF(..., 0)`，除数为 0 时返回 0 或 NULL，避免 SQL 报错。
  **统一算法**：`ROUND(attendedCount * 100.0 / NULLIF(attendedCount + absentCount, 0), 2)`
  - `attendedCount`：`attendance_status = 2` 的记录数；历史兼容时可把 `status = 4` 且 `attendance_status` 为空/0 的记录视为完成。
  - `absentCount`：`attendance_status = 3` 的记录数。
  - **不计入**：`status IN (0, 1, 2, 3)` 且 `attendance_status NOT IN (2, 3)` 的记录；取消、待支付、已支付、进行中都不是出勤率分母。
  - **预约状态码禁止改义**：`biz_course_booking.status=4` 在当前项目中是“已完成”，不是“缺席”。
- 所有新方法在 `CourseBookingMapper` 注释中标明「仅用于教练端」，防止以后在管理员/会员接口里被错误复用导致性能问题。
- **风险学员标记逻辑**（后端统一计算，前端禁止重复实现）：
  ```java
  isRisk = (attendanceRate < riskAttendanceRate) 
           || (lastLessonTime != null && lastLessonTime.isBefore(LocalDate.now().minusDays(riskInactiveDays)))
  ```
  其中 `riskAttendanceRate`、`riskInactiveDays` 从配置读取（见 P0 决策表）。

### 1.6 考勤字段写入链路（必须随 P1/P2 同步落地）
新增字段不是只供查询使用，必须在现有教练签到/完成链路中同步维护：

| 操作入口 | 当前预约状态变化 | 新考勤字段写入 | 实际时间字段 |
|----------|------------------|----------------|--------------|
| 教练点击「签到」 | `status: 2 → 3` | `attendance_status = 1` | `actual_checkin_time = NOW()`，仅当原值为空时写入 |
| 教练点击「完成」 | `status: 3 → 4` | `attendance_status = 2` | `actual_finish_time = NOW()`，仅当原值为空时写入 |
| 教练点击「缺席/取消」且课程已开始或教练明确选择缺席 | `status` 可保持业务允许的取消流转或置 `0` | `attendance_status = 3` | `actual_finish_time` 不写；`cancel_time` 仍按取消逻辑写 |
| 学员/系统取消待上课预约 | `status = 0` | 保持 `attendance_status = 0`，不得自动算缺席 | `cancel_time = NOW()` |

实现要求：
- `CourseBooking` 实体新增：
  ```java
  private Integer attendanceStatus;

  @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
  private LocalDateTime actualCheckinTime;

  @JsonFormat(pattern = "yyyy-MM-dd HH:mm:ss")
  private LocalDateTime actualFinishTime;
  ```
- `CourseBookingMapper#findById / findByIdAndCoachId / findAllByCoachId / findStudentScheduleForCoach`
  必须返回上述 3 个字段，确保前端可区分预约状态与考勤状态。
- `CourseBookingMapper` 新增或改造状态更新方法，不再只更新 `status/remark/update_time`：
  ```java
  int updateCoachBookingStatusAndAttendance(
      @Param("id") Long id,
      @Param("status") Integer status,
      @Param("attendanceStatus") Integer attendanceStatus,
      @Param("remark") String remark,
      @Param("writeCheckinTime") boolean writeCheckinTime,
      @Param("writeFinishTime") boolean writeFinishTime
  );
  ```
- `CourseBookingServiceImpl#updateStatusForCoach` 必须把 `status=3` 映射为 `attendanceStatus=1`，
  `status=4` 映射为 `attendanceStatus=2`；缺席不得复用 `status=4`，需要独立参数或独立接口明确表达。
- 如首版不想新增缺席接口，前端按钮文案应从「缺席/取消」拆成「取消」和「登记缺席」，避免一个按钮触发两种业务含义。

#### 1.6.1 实际落地情况（2026-07-13）

| 项 | 状态 | 实际实现 |
|----|------|----------|
| `CourseBooking` 实体三字段 | ✅ | 已新增 |
| 考勤写入 | ✅ | 独立 `updateAttendanceForCoach(AttendanceCommand)`，action = CHECK_IN / COMPLETE / ABSENT |
| 状态更新方法 | ✅ | `CourseBookingMapper` 条件更新 `attendance_status` + 实际时间字段 |
| 取消 vs 缺席分离 | ✅ | 课程开始前取消走 `/for-coach/status`；缺席走考勤 action |
| 缓存失效挂钩 | ✅ | `CoachStudentRelationCacheInvalidator` 在预约/课程/会员变更时清缓存 |

---

## 2. P2 · 后端 Service + Controller + 权限

### 2.1 目标
把 P1 的 SQL 包装成**安全、可缓存、前端可直接消费**的 REST 接口。

### 2.2 涉及文件

> **状态：✅ 已完成（2026-07-13）** — 接口独立为 `CoachStudentController`，未修改 `CoachController`。

- **新增**（已完成）：
  - `src/main/java/com/badminton/bmp/modules/coach/controller/CoachStudentController.java` — 6 个 REST 端点
  - `src/main/java/com/badminton/bmp/modules/coach/service/CoachStudentService.java`
  - `src/main/java/com/badminton/bmp/modules/coach/service/impl/CoachStudentServiceImpl.java`
  - `src/main/java/com/badminton/bmp/modules/coach/service/CoachStudentRelationService.java`
  - `src/main/java/com/badminton/bmp/modules/coach/service/impl/CoachStudentRelationServiceImpl.java`
  - `src/main/java/com/badminton/bmp/modules/coach/support/CoachStudentQueryRateLimiter.java` — 列表 5/s、详情 10/s
  - `src/main/java/com/badminton/bmp/modules/coach/support/CoachStudentLastSuccessCache.java` — DB 故障降级
  - `src/main/java/com/badminton/bmp/modules/coach/util/CoachStudentPrivacyUtils.java` — 脱敏/尾号
  - `src/main/java/com/badminton/bmp/modules/coach/dto/*`、`vo/*` — 查询参数与返回对象
  - `src/main/java/com/badminton/bmp/modules/course/cache/CoachStudentRelationCacheInvalidator.java`
- **复用**（未改接口签名）：
  - `CoachService.getCurrentCoachIdOrNull()` — 从登录态解析 coachId
- **实现差异**（可接受）：
  - `ensureCoachCanViewStudent` 为 `CoachStudentServiceImpl` 私有方法，非 `CoachStudentRelationService` 公开方法
  - 关系缓存用手动 `CacheManager`（cache 名 `coachStudentRelation`），非 `@Cacheable` 注解；TTL 由 `RedisCacheConfig` / `SimpleCacheConfig` 配置

### 2.3 新增接口清单（全部 `@PreAuthorize("hasRole('COACH')")`）

| 方法 | Path | 用途 | 对应 Mapper | 越权校验 |
|------|------|------|-------------|---------|
| GET | `/api/coach/students` | 教练学员列表（分页 + 搜索 + 筛选 + 排序） | `findDistinctStudentsByCoachId` + count | coachId 从登录态拿（复用 `getCurrentCoachIdOrNull`） |
| GET | `/api/coach/students/{id}` | 学员详情（基础档案 + KPI 聚合 + 等级/到期；余额为 P0 审批后的可选字段） | member `findById` + `getStudentCoachRelationStats` | **必须 `isStudentTaughtByCoach(coachId, id)`**，否则 403 |
| GET | `/api/coach/students/{id}/progress` | 训练进度（按课程名分组） | `getStudentProgressByCourseNameForCoach` | 同上越权校验 |
| GET | `/api/coach/students/{id}/schedule` | 课程安排（时间区间 + status + 分页） | `findStudentScheduleForCoach` | 同上越权校验 |
| GET | `/api/coach/students/{id}/attendance` | 出勤记录（时间线 + 月度汇总） | `findStudentAttendanceTimelineForCoach` + 月度聚合 | 同上越权校验 |
| GET | `/api/coach/students/{id}/consume-records` | 仅查看**与当前教练课程相关**的消费记录 | 新增教练范围消费查询，禁止直接透传通用 member 全量消费接口 | 同上越权校验 + 原接口分页 |

> **接口落地状态：6/6 ✅ 全部已实现并通过单测/契约测覆盖。**

> 隐私边界：首版不向教练开放学员充值记录明细；若详情页展示余额/累计消费，必须由产品确认字段范围。`totalRecharge` 默认不返回，除非后续 P0 单独评审通过。

### 2.3.1 参数校验与接口防护（六道防线落地）

| 防线 | 要求 | 失败处理 |
|------|------|----------|
| 参数校验 | `memberId/id/courseId > 0`；`page >= 1`；`1 <= size <= 100`；`keyword` trim 后最长 50；`status IN (0,1,2,3,4)`；`attendanceStatus IN (0,1,2,3)`；`orderBy` 仅允许 `lastLessonTime/attendanceRate/totalPaidAmount/totalHours`；日期区间最大 180 天 | 参数非法返回 400/业务错误码，不进入 Mapper |
| 业务规则 | 当前登录用户必须已绑定教练档案；学员未逻辑删除；课程/预约未逻辑删除；时间区间 `start <= end` | 返回业务错误，不暴露 SQL 或内部异常 |
| 权限校验 | 列表只能使用当前 coachId；所有 `{id}` 端点必须先调 `ensureCoachCanViewStudent` | 无关系返回 403 |
| 幂等处理 | GET 接口天然幂等；登记签到/完成/缺席沿用预约状态机，必须基于旧状态条件更新 | 重复完成/重复缺席返回明确业务提示 |
| 请求限流 | `{id}` 详情类接口按 `coachId + memberId` 限制 10 次/秒；列表接口按 `coachId` 限制 5 次/秒；异常高频 memberId 枚举进入告警 | 超限返回 429 或项目统一错误码 |
| 降级与异常 | `progress/attendance` 缓存失效或慢查询时降级为近 90 天；消费记录接口不可降级为全量 member 查询 | 返回空分页或降级提示，不返回敏感全量数据 |

### 2.4 Service 层要点
1. **越权复用 + 缓存优化**：抽 `canCoachViewStudent(Long coachId, Long memberId)` 做 boolean 缓存；
   再由 `ensureCoachCanViewStudent(Long coachId, Long memberId)` 统一抛 `AccessDeniedException`。
   所有 `{id}` 端点首行调用 `ensure...`，避免 5 个方法各自复制。
   ```java
   // 在 CoachStudentRelationServiceImpl 中定义；避免 CoachServiceImpl 内部自调用导致 @Cacheable 代理不生效。
   @Cacheable(value = "coach:student:relation", key = "#coachId + ':' + #memberId")
   public boolean canCoachViewStudent(Long coachId, Long memberId) {
       if (coachId == null || memberId == null) return false;
       return courseBookingMapper.isStudentTaughtByCoach(coachId, memberId) > 0;
   }

   public void ensureCoachCanViewStudent(Long coachId, Long memberId) {
       if (!canCoachViewStudent(coachId, memberId)) {
           throw new AccessDeniedException("无权查看该学员信息");
       }
   }
   
   // 缓存 false 也有价值，可缓解恶意枚举 memberId 导致的穿透；TTL 统一 5 分钟。
   // 缓存失效时机：学员新增/取消预约、教练离职/解绑、会员注销/逻辑删除。
   @CacheEvict(value = "coach:student:relation", key = "#coachId + ':' + #memberId")
   void evictStudentRelationCache(Long coachId, Long memberId);
   ```
2. **高风险标记统一算**：`isRisk = attendanceRate < 70 OR lastLessonTime 超过 14 天`，
   在 Service 层统一写入返回对象，**禁止前端各自实现**（否则不同页面对同一学员显示结果不一致）。
   ```java
   // 读取配置
   @Value("${coach.risk-threshold.attendance-rate:70}")
   private int riskAttendanceRate;
   
   @Value("${coach.risk-threshold.inactive-days:14}")
   private int riskInactiveDays;
   
   // 统一判断逻辑；attendanceRate 为 null 时按 0 处理，但不要把无历史课的学员误判为低出勤。
   private boolean isRiskStudent(CoachStudentItem student) {
       if (student.getAttendedCount() + student.getAbsentCount() > 0
               && student.getAttendanceRate() < riskAttendanceRate) {
           return true;
       }
       if (student.getLastLessonTime() == null) return false;
       LocalDate lastLesson = student.getLastLessonTime().toLocalDate();
       return lastLesson.isBefore(LocalDate.now().minusDays(riskInactiveDays));
   }
   ```
3. **缓存策略**：对 `progress`、`attendance` 等查询较重的接口，加基于 `coachId + memberId`
   的 `@Cacheable`（参考 `CourseEntityCache` 模式）；并在
   `CourseBookingService#updateStatusForCoach` 成功路径里清对应缓存。
4. **字段裁剪**：
   - 手机号原样返回（教练要拨打电话，前端统一脱敏展示）；
   - 身份证号仅返末 4 位（`idCardTail`），不返整串；
   - `avatar` 为空时由 Service 层替换为前端可识别的占位常量 URL（保持与教练头像占位一致的命名）。
   - 充值记录明细默认不返；`balance/totalRecharge` 属于账户财务信息，必须按 P0 评审结果决定是否返回。若未评审通过，详情页只返回与当前教练课程相关的 `totalPaidAmount/totalConsumptionForCoach`。

#### 2.4.1 实际落地情况（2026-07-13）

| P0 冻结项 | 状态 | 验证点 |
|-----------|------|--------|
| 列表 `maskedPhone` / 详情完整 `phone` | ✅ | `CoachStudentListItemVO.maskedPhone`；`CoachStudentDetailVO.phone` |
| 身份证仅 `idCardTail` | ✅ | `CoachStudentDetailVO.idCardTail` |
| 不返回 balance / totalRecharge | ✅ | VO 无此字段 |
| 教练范围消费 | ✅ | `findConsumeRecordsForCoach`，非 member 全量接口 |
| 180 天关系保留 | ✅ | `RELATION_RETENTION_DAYS = 180` |
| 风险标记后端统一计算 | ✅ | `isRisk()` + `coach.risk-threshold.*` 配置 |
| 越权 403 | ✅ | `AccessDeniedException`，Controller 未捕获转换 |
| DB 故障降级 | ✅ | `CoachStudentLastSuccessCache` |
| 请求限流 | ✅ | `CoachStudentQueryRateLimiter` |

**单测覆盖：** `CoachStudentControllerTest`、`CoachStudentRelationServiceTest`、`CoachStudentPrivacyAndValidationTest`、`CoachStudentFallbackTest` 等 9 个测试类。

---

## 3. P3 · 前端 API 封装 + TS 类型

### 3.1 目标
页面层可以像调用 `getMyCourses` 一样直接调用 5 个学员方法，**消灭零散 fetch**。

### 3.2 涉及文件

> **状态：✅ 已完成（2026-07-13）** — 实际 6 个请求函数（含 consume-records），类型独立文件。

- 修改：
  - `BMP-uniapp/config/api.ts` — `COACH.STUDENTS` 等 6 个路径常量
  - `BMP-uniapp/api/coachSelf.ts` — 6 个请求函数（`suppressErrorToast: true`）
- 新增：
  - `BMP-uniapp/types/coachStudents.ts` — TS 类型（原计划放 `coachSelf.ts` 底部，实际独立文件）
  - `BMP-uniapp/tests/coachStudentsApi.spec.ts` — API 路径与参数契约测试

### 3.3 TS 类型（`types/coachStudents.ts`，原计划放 `coachSelf.ts` 底部）

```ts
// 列表行（含 KPI）
export interface CoachStudentItem {
  memberId: number
  memberName: string
  avatar?: string
  gender?: number
  phone?: string               // 后端原号返回，前端统一脱敏展示（详情页"拨打确认弹窗"显示全号）
  memberType: 'NORMAL' | 'MEMBER'
  memberLevel?: number
  status?: number              // 0-冻结 1-正常 2-到期
  firstLessonTime?: string
  lastLessonTime?: string
  totalBookings: number
  attendedCount: number
  absentCount: number
  cancelledCount: number
  attendanceRate: number       // 0-100 百分比整数，前端直接显示
  totalPaidAmount: number
  isRisk?: boolean             // 后端算好：出勤率<70% 或 最近14天未上课
}

// 详情页主对象（合并档案 + 聚合）
export interface CoachStudentDetail extends CoachStudentItem {
  idCardTail?: string          // 末 4 位，例：1234
  registerTime?: string
  expireTime?: string
  balance?: number             // 账户财务字段，默认不返回；需 P0 单独确认后开启
  totalConsumption?: number    // 建议仅返回与当前教练课程相关的消费汇总
  totalRecharge?: number       // 默认不返回充值明细/汇总，除非产品与隐私评审通过
  kpi: {
    totalHours: number         // 累计课时（分钟→小时，前端直接展示）
    avgAttendance30d: number   // 近 30 日出勤率（0-100）
    rankAmongMyStudents?: number // 我带学员中的活跃度排名（可选增强）
  }
}

export interface CoachStudentProgressGroup {
  courseName: string
  attended: number             // 已完成节数
  total: number                // 总预约节数
  hours: number                // 累计课时（分钟，前端换算）
  lastLessonTime?: string
}

// 课程安排（补全字段，不完全复用 CoachBookingItem）
export interface CoachStudentScheduleItem {
  id: number
  bookingNo: string
  courseName: string
  courseDate: string
  startTime: string
  endTime: string
  courtName?: string
  venueName?: string
  status: number               // 预约状态：0-已取消 1-待支付 2-已支付 3-进行中 4-已完成
  paymentStatus?: number
  attendanceStatus?: number    // 考勤状态：0-未签到 1-已签到/进行中 2-已完成 3-缺席
  memberRemark?: string        // 学员备注（教练需要看到）
  coachRemark?: string         // 教练备注
  orderAmount?: number
  createTime?: string
  updateTime?: string
}

export interface CoachStudentAttendanceItem {
  date: string                 // yyyy-MM-dd
  courseName: string
  startTime?: string
  hours: number
  status: number               // 预约状态：0-已取消 1-待支付 2-已支付 3-进行中 4-已完成
  attendanceStatus?: number    // 考勤状态：0-未签到 1-已签到/进行中 2-已完成 3-缺席
  remark?: string
  bookingId: number
}
```

### 3.4 请求函数名（延续现 `getMyCourses / getCurrentCoach` 命名族）
- `getCoachStudents(params)` → `PageResult<CoachStudentItem>`
- `getCoachStudentDetail(memberId)`
- `getCoachStudentProgress(memberId)`
- `getCoachStudentSchedule(memberId, params)`
- `getCoachStudentAttendance(memberId, params)`
- `getCoachStudentConsumeRecords(memberId, params)`（仅查与当前教练课程相关的消费记录，禁止直接复用通用 member 全量接口）

> **落地状态：6/6 ✅** — `getCoachStudents` / `getCoachStudentDetail` / `getCoachStudentProgress` / `getCoachStudentSchedule` / `getCoachStudentAttendance` / `getCoachStudentConsumeRecords` 均已封装。

---

## 4. P4 · 页面注册 + 全局入口

### 4.1 目标
让教练**不超过 2 次点击**到达学员列表 / 详情。

### 4.2 涉及文件

> **状态：✅ 已完成（2026-07-13）** — 工具函数实际文件名为 `coachStudents.ts`（非 `coachView.ts`）。

- 修改：
  - `BMP-uniapp/pages.json` — 注册 `student-list` / `student-detail` ✅
  - `BMP-uniapp/pages/coach/index.vue` — 全宽「我的学员」卡片 ✅
  - `BMP-uniapp/components/coach/CoachTabBar.vue` — 保持 4 项，未新增 Tab ✅
  - `BMP-uniapp/utils/coachStudents.ts` — URL 构造 + `maskPhone` + 头像兜底（原计划 `coachView.ts`）
  - `BMP-uniapp/pages/coach/students.vue` — 保留单课签到，新增「查看学员详情 →」✅
- 新增：
  - `BMP-uniapp/pages/coach/student-list.vue` ✅
  - `BMP-uniapp/pages/coach/student-detail.vue` ✅
  - `BMP-uniapp/tests/coachStudentsUi.spec.ts` — URL/脱敏/页面源码契约测试

### 4.2.1 页面路径约定
- 课程内签到名单：`/pages/coach/students?courseId=...`，继续服务现有课程签到/状态更新流程。
- 我的学员全局列表：`/pages/coach/student-list`，从工作台全宽卡片进入。
- 学员详情：`/pages/coach/student-detail?id=...`（从全局列表或课程内签到名单进入；原计划 `memberId` 参数名，实际为 `id`）。

### 4.3 `coachStudents.ts` 新增工具（原计划 `coachView.ts`）

```ts
export function buildCoachStudentListUrl(keyword?: string, riskOnly?: boolean): string

export function buildCoachStudentDetailUrl(
  memberId: number | string,
  memberName?: string
): string

// 通用展示工具：
/**
 * 手机号脱敏工具（统一前端展示逻辑）
 * @param phone 原始手机号
 * @param showFull 是否显示全号（仅在"拨打确认弹窗"中传 true）
 * @returns 脱敏后的手机号或全号
 */
export function maskPhone(phone?: string, showFull = false): string {
  if (!phone) return '未填写'
  if (showFull) return phone  // 仅在拨打确认弹窗中展示全号
  if (phone.length === 11) {
    return `${phone.slice(0, 3)}****${phone.slice(7)}`  // 138****1001
  }
  return phone  // 非标准11位号码（虚拟号）原样返回
}

/**
 * 出勤率徽章（统一颜色分级）
 * @param rate 出勤率 0-100
 * @returns { text: '高/中/低', klass: 'badge-high/badge-medium/badge-low' }
 */
export function attendanceBadge(rate: number): { text: string; klass: string } {
  if (rate >= 80) return { text: '高', klass: 'badge-high' }
  if (rate >= 60) return { text: '中', klass: 'badge-medium' }
  return { text: '低', klass: 'badge-low' }
}

/**
 * 学员头像回退工具（统一处理头像加载失败）
 * @param avatar 原始头像URL
 * @param memberName 学员姓名（用于生成首字母占位符，可选）
 * @returns 有效的头像URL或默认占位符
 */
export function getStudentAvatarWithFallback(avatar?: string, memberName?: string): string {
  if (!avatar) {
    // 如果有会员名，可以用首字母生成占位头像（未来可接入头像生成服务）
    return memberName 
      ? `${DEFAULT_COACH_STUDENT_AVATAR}?name=${encodeURIComponent(memberName[0])}`
      : DEFAULT_COACH_STUDENT_AVATAR
  }
  return avatar
}

/**
 * 统一学员头像占位符常量（必须全局唯一，避免多套默认图混用）
 */
export const DEFAULT_COACH_STUDENT_AVATAR = '/static/placeholders/avatar.svg'
```

> **实际落地：** 占位图统一为 `/static/placeholders/avatar.svg`（P0 冻结覆盖说明已明确，非 `avatar-student.svg`）。
> `buildCoachStudentListUrl()` 当前仅返回固定路径，尚未支持 `keyword` / `riskOnly` 预填参数。

> **关键规范**：
> 1. `maskPhone()` 在列表页、详情页顶卡中调用时**不传第二参数**（默认脱敏）；
>    仅在「一键拨打电话」的二次确认弹窗中传 `showFull: true`。
> 2. `DEFAULT_COACH_STUDENT_AVATAR` 必须统一常量、所有学员头像渲染统一使用，
>    **避免 634518 号教训中「多套默认图混用 + 图片 404 无回退」**。
> 3. 头像组件必须同时具备 `src` 回退和 `@error` 回退：
>    ```vue
>    <image 
>      :src="getStudentAvatarWithFallback(item.avatar, item.memberName)" 
>      @error="handleAvatarError"
>      mode="aspectFill"
>    />
>    
>    <script setup>
>    function handleAvatarError(e) {
>      e.target.src = DEFAULT_COACH_STUDENT_AVATAR
>    }
>    </script>
>    ```

### 4.4 工作台入口布局（基于现有代码优化）

**现状分析**（基于 `pages/coach/index.vue`）：
- 当前工作台采用 **3个横向action-card** 布局（`grid-template-columns: repeat(3, minmax(0, 1fr))`）
- 卡片依次为：我的课程（渐变主色）、预约明细、我的档案

**优化方案**（最小变更原则）：
在现有 `.actions-row` 下方新增 `.student-card-section`，避免破坏现有布局：

```vue
<!-- 在 actions-row 后新增 -->
<view class="student-card-section" @tap="handleQuickAction('students')">
  <view class="student-card-content">
    <view class="student-card-left">
      <text class="student-card-title">我的学员</text>
      <text class="student-card-desc">已服务 {{ metrics.totalStudents }} 人</text>
    </view>
    <view class="student-card-icon">
      <uni-icons type="person-filled" size="28" color="#ff6600" />
    </view>
  </view>
</view>
```

同时把 `handleQuickAction` 的类型从 `'courses' | 'bookings' | 'profile'`
扩展为 `'courses' | 'bookings' | 'profile' | 'students'`，并将 `students`
映射到 `buildCoachStudentListUrl()`，不要复用 `/pages/coach/students`。

```scss
.student-card-section {
  margin-top: 24rpx;
  border-radius: 22rpx;
  background: linear-gradient(135deg, #fff5f0 0%, #ffffff 100%);
  padding: 32rpx 26rpx;
  box-shadow: 0 8rpx 24rpx rgba(163, 62, 0, 0.08);
}

.student-card-content {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.student-card-left {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.student-card-title {
  font-size: 40rpx;
  font-weight: 900;
  color: #1a1c1c;
}

.student-card-desc {
  font-size: 24rpx;
  color: #a33e00;
  font-weight: 600;
}

.student-card-icon {
  width: 80rpx;
  height: 80rpx;
  border-radius: 20rpx;
  background: #ffdbcd;
  display: flex;
  align-items: center;
  justify-content: center;
}
```

**为何不改为2×2网格**：
- 现有3个卡片已形成**视觉节奏**（主色卡+2个白卡），改为2×2会破坏平衡
- "我的档案"是低频入口，与"学员管理"优先级不对等，不适合并排
- 全宽卡片能容纳更多信息（如"高风险学员X人"等未来扩展字段）

---

## 5. P5 · 前端 UI 两页（student-list + student-detail）

**两页通用约束**：
1. 必须具备 加载 / 失败 / 空态 三态；
2. 顶部使用 `CoachTopBar`（复用），底部使用 `CoachTabBar`；
3. 均开启 `refresher` 下拉刷新 + `onShow` 自动重拉；
4. 头像组件统一走：
   ```vue
   <image 
     :src="getStudentAvatarWithFallback(item.avatar, item.memberName)" 
     @error="e => e.target.src = DEFAULT_COACH_STUDENT_AVATAR"
     mode="aspectFill"
   />
   ```
   **双重兜底机制**：工具函数兜底 + onerror 兜底，确保任何情况下都不出现破图。
5. 手机号展示统一调 `maskPhone()`（详情页「一键拨打」按钮的确认弹窗中传 `showFull: true`）：
   ```ts
   // 列表页、详情页顶卡
   <text>{{ maskPhone(student.phone) }}</text>  // 138****1001
   
   // 拨打电话二次确认弹窗
   uni.showModal({
     title: '确认拨打',
     content: `即将拨打 ${maskPhone(student.phone, true)}`,  // 13812341001
     confirmText: '拨打',
     success: (res) => {
       if (res.confirm) {
         uni.makePhoneCall({ phoneNumber: student.phone })
       }
     }
   })
   ```

---

### 5.1 `student-list.vue` 教练学员列表

#### 结构（与 `courses.vue` 视觉族保持一致）
- **Hero 头**：`MY STUDENTS` + 副标题「已服务 N 人 · 平均出勤率 XX%」
  （两个指标在列表接口首条响应里补充聚合字段，或单独调一次 `/stats` 小接口）
- **筛选卡**：
  - 搜索框：姓名 / 手机号
  - Tab Chip：全部（默认） · 高风险缺勤 · 今日有课 · 会员 · 普通
  - 排序选择：最近上课 / 出勤率低到高 / 累计消费高到低 / 累计课时高到低
- **卡片列表**：
  - 左：头像（常量兜底 + onerror 兜底常量）
  - 中：姓名 + 脱敏手机 + 会员等级标签 + 最近上课时间
  - 右：出勤率 Ring / Pill + 风险徽章（高风险）
  - 底部小字：累计课时 h · 累计消费 ¥
- **分页卡**：复用 `courses.vue` 的分页样式

#### 空态/失败态
- 空态：「还没有学员，等学员预约你的课程后会出现在这里」+ 跳「我的课程」引导按钮
- 失败态：统一卡片 + 重试按钮（不可无限重试，最多 3 次/次会话）

---

### 5.2 `student-detail.vue` 学员详情（四 Tab）

顶部信息卡 + 四个 Tab：**概览 / 训练进度 / 课程安排 / 出勤记录**。

#### 顶卡
- 头像 + 姓名 + 电话拨盘（`uni.makePhoneCall`，拨出前二次确认）
- 会员徽章 Lv.X + 到期时间（30 天内到期 → 橙色提示、已过期 → 红色提示）
- 余额小条（可选，需 P0 明确允许教练查看账户余额；未获批时不展示）
- 风险条：若 `isRisk`，顶部红色横条文案「近 30 日出勤率 < 70% 或 14 天未上课」

#### Tab1 · 概览
4 张 KPI 卡：
1. 累计课时（小时）
2. 近 30 日出勤率
3. 累计消费
4. 账户余额（可选；未获批时替换为“我带课消费”或隐藏）

下方：
- "最近一次上课" + "下一次已约课程"小时间线；
- 近 7 次上课 mini 时间线（点击跳 Tab4）。

#### Tab2 · 训练进度（按课程名聚合）
- 每行：课程名 + 进度条 `attended / total` + 小时数 + 最近上课；
- 若某学员只有单次课、`total = 0`：显示「暂无系统课包进度」而非空态，
  避免教练误以为系统 bug。
- **页脚说明卡**（防止误解）：
  ```vue
  <view class="info-card">
    <uni-icons type="info" size="18" color="#ff6600" />
    <text class="info-text">
      进度基于课程名称自动聚合统计，非正式课程包。
      如需购买1v1私教包，请联系管理员开通正式课程包模块。
    </text>
  </view>
  ```
  样式参考iOS系统设置中的说明卡片（浅橙背景 + 小图标 + 灰色文字）。

#### Tab3 · 课程安排
两个子 Tab：
- **即将上课**：`status IN (1, 2, 3)` 且 `attendance_status NOT IN (2, 3)`，按课程日期+开始时间升序。
- **历史课程**：`status IN (0, 4)` 或 `attendance_status IN (2, 3)`，按课程日期+结束时间倒序。

列表卡片**复用 `bookings.vue` 的样式**；点击可跳原课程预约详情（复用 `/for-coach/{id}` 接口），不重复造轮子。

#### Tab4 · 出勤记录
- 月度汇总小条：本月出勤 X / 缺席 Y / 出勤率 Z%
- 时间线：日期 + 课程名 + 预约状态 Pill（已取消 / 待支付 / 已支付 / 进行中 / 已完成）+ 考勤状态 Pill（未签到 / 已签到 / 已完成 / 缺席）+ 小时数 + 备注

### 5.3 实际落地情况（2026-07-14）

> **整体：✅ 功能代码完成（100%）。** P5 收尾已通过提交 `0f454b7` 合并到 `main`；上线前仍需完成真机 4 case 验收。

#### `student-list.vue` — ✅ 100%

| 计划项 | 状态 |
|--------|------|
| Hero 头 + 汇总指标 | ✅ `已服务 N 人 · 平均出勤率 XX%` |
| 搜索 + 5 个 Chip 筛选 | ✅ 全部/高风险/今日有课/会员/普通 |
| 4 种排序 | ✅ |
| 卡片列表 + 分页 | ✅ |
| 加载/失败/空态三态 | ✅ 失败重试上限 3 次 |
| refresher + onShow | ✅ |
| 列表手机号脱敏 | ✅ 直接用后端 `maskedPhone` |
| 头像双重兜底 | ✅ `getStudentAvatarWithFallback` + `@error` |
| URL 条件预填 | ✅ `buildCoachStudentListUrl` + `parseCoachStudentListRoute` 支持 `keyword/riskOnly` |

#### `student-detail.vue` — ✅ 100%

| 计划项 | 状态 | 备注 |
|--------|------|------|
| 五 Tab 骨架 | ✅ | 概览/训练进度/课程安排/出勤记录/消费记录 |
| 风险横幅 | ✅ | |
| 顶卡脱敏手机 + 拨打确认 | ✅ | 顶卡脱敏；确认弹窗统一走 `maskPhone(phone, true)` |
| 会员到期提示 | ✅ | 30 天内橙色、已过期红色 |
| 余额小条 | ⬜ 跳过 | P0 未开放，符合预期 |
| Tab1 概览 KPI | ✅ | 新增「近 30 日出勤率」计算展示 |
| Tab1 近 7 次 mini 时间线 | ✅ | 点击趋势卡可切换到出勤记录 Tab |
| Tab2 进度条 + 说明卡 | ✅ | |
| Tab3 即将/历史子 Tab | ✅ | 按预约状态 + 考勤状态分类；即将升序、历史倒序 |
| Tab3 点击跳预约详情 | ✅ | `/pages/coach/bookings?id=` 已解析 `id` 并自动打开详情 |
| Tab4 月度汇总 | ✅ | 按月显示完成数、缺席数及出勤率 |
| Tab4 时间线 | ✅ | 预约状态 + 考勤状态 |
| 消费记录展示 | ✅ | 成功接入第 5 个 Tab 展示消费记录 |

**P5 自动化验证（2026-07-14）：** `npm test` 24/24、`npm run type-check` 通过、微信小程序构建通过；`coachStudentsUi.spec.ts` 已覆盖状态分类、月度出勤率、深链、趋势跳转和路由预填。

---

## 6. P6 · 实时推送 + 测试 + 边界校验

> **状态：❌ WebSocket 未启动 · ⚠️ 主体单测已覆盖，但部分计划中的契约/迁移测试文件不存在**

### 6.0 测试落地情况（2026-07-14）

| 类别 | 文件 | 状态 |
|------|------|------|
| Controller | `CoachStudentControllerTest` | ✅ |
| 关系校验 | `CoachStudentRelationServiceTest` | ✅ |
| 隐私/参数 | `CoachStudentPrivacyAndValidationTest` | ✅ |
| 降级缓存 | `CoachStudentFallbackTest` | ✅ |
| Mapper 契约 | `CoachStudentMapperContractTest` | ❌ 文件未落地 |
| 基础设施 | `CoachStudentInfrastructureContractTest` | ❌ 文件未落地 |
| 限流 | `CoachStudentQueryRateLimiterTest` | ✅ |
| DDL 脚本 | `CoachStudentsMigrationSqlTest` | ❌ 文件未落地 |
| 缓存挂钩 | `CoachStudentRelationCacheHookContractTest` | ❌ 文件未落地 |
| 考勤链路 | `CourseBookingAttendanceServiceTest`、`CourseBookingControllerAttendanceTest` | ✅ |
| 前端 API | `coachStudentsApi.spec.ts` | ✅ |
| 前端 UI 契约 | `coachStudentsUi.spec.ts` | ✅ |
| 双账号 E2E | — | ❌ 未实现 |

**当前可复现测试结果（2026-07-14）：** 后端 `mvn test` 39/39 通过；前端 `npm test` 24/24 通过。该结果不等价于生产数据库迁移验证、Mapper 真库集成测试或双账号 E2E。

### 6.1 WebSocket（P6，**不影响 P1~P5 独立上线**）

> **状态：❌ 未实现** — `WebSocketPushService` 已存在于项目中（场地/装备等模块使用），但 `CourseBookingServiceImpl` 未推送 `COACH_STUDENT_*` 事件；前端无 `coach-ws` 订阅模块。首版依赖 `onShow + refresher` 兜底，符合 P0 冻结决策。
#### 涉及文件
- `src/main/java/com/badminton/bmp/websocket/WebSocketPushService.java`
- `src/main/java/com/badminton/bmp/modules/course/service/impl/CourseBookingServiceImpl.java`
  （`updateStatusForCoach` 成功路径）
- 前端：`store/modules/user.ts`（可在同级新增一个 coach-ws 订阅模块；仅当角色 = COACH 时订阅）

#### 新增事件常量（建议枚举）
| 事件 key | 触发点 | 携带 payload | 优先级 |
|----------|--------|--------------|--------|
| `COACH_STUDENT_CANCELLED` | 学员取消我的课 | memberId、bookingId、courseName | **HIGH（立即弹窗+震动）** |
| `COACH_STUDENT_NEW_BOOKING` | 新学员报名我的课 | memberId、courseId、courseName | MEDIUM（Badge提示） |
| `COACH_STUDENT_ATTENDANCE_CHANGED` | 签到/完成/缺席变化 | memberId、bookingId、bookingStatus、attendanceStatus | LOW（仅刷新列表） |
| `COACH_MEMBER_STATUS_CHANGED`（可选） | 会员到期/冻结 | memberId、新状态 | LOW |

#### 前端订阅处理（分级响应策略）
```ts
// 在 store/modules/coach-ws.ts 中定义
const WS_EVENT_PRIORITY = {
  COACH_STUDENT_CANCELLED: 'HIGH',
  COACH_STUDENT_NEW_BOOKING: 'MEDIUM',
  COACH_STUDENT_ATTENDANCE_CHANGED: 'LOW',
  COACH_MEMBER_STATUS_CHANGED: 'LOW'
}

function handleCoachEvent(event: CoachWSEvent) {
  const priority = WS_EVENT_PRIORITY[event.type]
  
  // HIGH优先级：震动+弹窗提醒（学员取消课程属于紧急事件）
  if (priority === 'HIGH') {
    uni.vibrateShort()
    uni.showModal({
      title: '学员取消预约',
      content: `${event.memberName} 取消了 ${event.courseName}`,
      showCancel: false
    })
    refreshCurrentPage()
  }
  
  // MEDIUM优先级：Badge提示（不打断当前操作）
  if (priority === 'MEDIUM') {
    showBadge('students', '+1')
    // 仅当用户在学员列表页时才自动刷新
    if (getCurrentRoute() === '/pages/coach/student-list') {
      refreshCurrentPage()
    }
  }
  
  // LOW优先级：静默刷新（仅当用户在相关页面时）
  if (priority === 'LOW') {
    if (getCurrentRoute().includes('student')) {
      refreshCurrentPage()
    }
  }
}

// 防抖合并（200ms内同一coachId的多个事件合并处理，防止推送风暴）
const debouncedRefresh = debounce(refreshCurrentPage, 200)
```

### 6.2 测试矩阵（最小必测）
#### 后端单元 / 集成
- **越权用例**：A 教练伪造 B 教练学员的 memberId 调 5 个 `{id}` 端点 → 必须 403
- **出勤率边界**：学员只有 0 条预约 → 出勤率 = 0 / NULL，接口不抛 500
- **预约状态码兼容**：`status=4` 必须显示为“已完成”，不得显示为“缺席”
- **考勤状态统计**：`attendance_status=2` 计入完成，`attendance_status=3` 计入缺席；取消/待支付/已支付/进行中不进入出勤率分母
- **考勤写入链路**：教练签到后 `attendance_status=1` 且 `actual_checkin_time` 有值；教练完成后 `attendance_status=2` 且 `actual_finish_time` 有值；登记缺席后 `attendance_status=3` 且不误写为 `status=4`
- **历史回填**：旧数据 `status=4` 且 `attendance_status` 为空/0 → 回填为 `attendance_status=2`；取消记录不得自动回填为缺席
- **关键词搜索**：姓名 / 手机号同时命中；空串 / 空格 keyword 行为与现 `findAll` 一致
- **分页**：page<1、size>100 的降级逻辑与现 `findAllForCoach` 一致
- **参数防护**：非法 `memberId/status/attendanceStatus/orderBy/dateRange` 必须在 Controller/Service 层拦截，不得进入 Mapper
- **缓存有效性**：连续 2 次访问同一 `{coachId}:{memberId}` 关系，第二次应命中 `CoachStudentRelationService` 缓存；测试覆盖 Spring 代理调用而非同类自调用
- **消费记录边界**：教练只能看到与自己课程相关的消费记录；不得返回该学员其他场馆消费、充值明细、全量余额流水

#### 前端验证
- TS 类型检查（项目现 `type-check-errors.txt` 同工具）：**不得新增 error**
- 头像 404 场景：
  1. 手动将接口 avatar 字段改成 `null` → 必须显示默认 SVG
  2. 手动将 avatar 改成 `https://invalid-url.com/404.jpg` → `@error` 触发后显示默认 SVG
  3. 默认 SVG 本身不存在 → 组件应有最终兜底（纯色占位符）
- 手机号脱敏：
  - 11 位数字（`13812341001`）→ `138****1001`
  - 非 11 位（虚拟号 `95123456`）→ 原样返回 `95123456`
  - 空值 → 显示 `未填写`
- 拨打电话确认弹窗：
  1. 点击"一键拨打"按钮 → 弹窗显示完整号码
  2. 确认后 → 调用 `uni.makePhoneCall`
  3. 取消 → 无任何操作

### 6.3 教练端 vs 管理员端边界（PR Checklist 必勾）
- `pages/coach/student-list.vue`、`pages/coach/student-detail.vue` **严禁出现**「新增 / 编辑 / 删除 / 冻结会员」按钮（教练只读）；
- 严禁在 coachSelf.ts 中封装 `/api/member/add` `/update` `/delete` 调用；
- 页面里任何"修改"入口，只允许跳转至管理员端的"联系管理员"提示，**不得跳过前端按钮来绕过**。

---

## 7. 交付顺序 & 里程碑（按 PR 切分，便于 Review + 回滚）

| PR | 阶段 | 代码量估 | 能否独立上线 | 自测手段 | **状态（2026-07-14）** |
|----|------|---------|-------------|---------|------------------------|
| 1 | P1 + DDL patch（考勤字段 / 索引 / 实体 / Mapper / 状态写入链路） | 中 | ✅ 接口未暴露，安全 | 本地 DB 跑 DDL 幂等校验、`@Select` / 手写小 SQL 验证；教练签到/完成写入考勤字段 | ✅ **已完成** |
| 2 | P2（Service + Controller） | 中 | ✅ 前端未接入 | Swagger（`OpenApiConfig` 已接入）测 6 个端点 + 越权 | ✅ **已完成** |
| 3 | P3（TS API 封装 + 类型） | 小 | ✅ 无 UI 变更 | 类型检查通过 + 控制台手动 1 条 | ✅ **已完成** |
| 4 | P4（入口 / 路由 / 工具） | 小 | ✅ 即使 UI 未完成也能看到入口不崩 | 手动走：教练工作台 → 学员卡 → 白页 list 不报错 | ✅ **已完成** |
| 5 | P5（list + detail 骨架 + 五 Tab 完稿） | 大 | ⚠️ 需与 PR3 一起 | 自动化测试已通过；真机走 4 条 case：有学员 / 空 / 风险 / 到期 | ✅ **代码完成，真机待验收** |
| 6 | P5（细节：头像兜底 + 脱敏 + 状态分类 + 深链 + 月度汇总） | 中 | ✅ | `coachStudentsUi.spec.ts` + 类型检查 + 小程序构建 | ✅ **已完成并合并**（`0f454b7`） |
| 7 | P6（WS + 单测 + 边界校验） | 中 | ✅ 可独立回退 | 双账号（教练 + 学员）取消/报名观察推送 | ❌ **核心未开始**（主体单测已有，WS/E2E/部分契约测试未做） |

**里程碑检查点：**
- **M1（PR1+PR2）**：✅ 代码与单测已覆盖；建议 Swagger 手动验收一次后归档
- **M2（PR5+PR6）**：✅ 功能代码及自动化验证完成；⚠️ 真机回归 4 case 后方可灰度
- **M3（PR7）**：❌ 待 P6 WebSocket 实现后执行

---

## 8. 关键风险与回滚预案

| 风险 | 触发条件 | 影响 | 回滚 / 预案 |
|------|---------|------|-------------|
| Mapper 新 SQL 在生产数据量下慢查询 | 某热门教练学员 > 5000、时间范围拉得太大 | 接口 5xx，教练工作台连带卡住 | 1. 严格分页，默认 size=20（列表），size=50（详情Tab），最大 size=100；2. P1 的考勤字段与关键索引必须先上（DBA 确认 EXPLAIN 结果）；3. 慢日志阈值设 500ms，超阈值立即降级到「仅查近 90 天预约」；4. 使用 `idx_cb_member_attendance` + `idx_c_coach_date_range` 加速出勤率和最近上课计算 |
| 预约状态码与考勤状态混用 | 开发者继续把 `status=4` 当“缺席”或用 `status=3` 当“已完成” | 出勤率、风险学员、时间线全部错误 | 1. Mapper 注释和 TS 类型必须同时写明两套状态；2. 单测覆盖 `status=4` 显示“已完成”；3. Code Review 检查所有缺席统计只能读取 `attendance_status=3` |
| 新增考勤字段没人维护 | 只建字段和查询，未改 `updateStatusForCoach` / Mapper 更新方法 | 新接口长期显示 0 出勤或历史数据正常、新数据异常 | 1. P1 必须同时改实体、Mapper、Service 状态写入；2. 签到/完成/缺席测试纳入 M1；3. 监控 `status=4 AND attendance_status<>2` 的异常计数 |
| 缓存注解因同类自调用失效 | `ensureCoachCanViewStudent` 和 `canCoachViewStudent` 放同一个 Service 内部调用 | 高频详情接口仍反复查库，缓存穿透防护失效 | 1. 独立 `CoachStudentRelationService` 承载 `@Cacheable`；2. 集成测试验证第二次命中缓存；3. 监控关系校验 SQL QPS |
| 越权逻辑漏洞 | `ensureCoachCanViewStudent` 被后续 refactor 误删或改条件 | 教练可查任意会员（含跨场馆/非学员） | P2 写越权单测并 CI 必跑；PR checklist 单独立一条「所有 {id} 接口首行调用 ensureCoachCanViewStudent」；**缓存失效机制**必须同步测试 |
| 越权校验缓存穿透 | 恶意用户频繁请求不存在的 memberId | 缓存无效，每次都查库，DB 压力飙升 | 1. 对不存在的关系也缓存（值为 `false`），TTL=5min；2. 接口层加限流（同一 coachId 每秒最多查询10个不同 memberId）；3. 监控 `isStudentTaughtByCoach` 的 cache miss 率，超 30% 报警 |
| 消费/充值隐私越界 | 直接复用通用 `MemberService#getConsumeRecords` 或充值接口 | 教练看到与自己无关的消费、充值流水或账户隐私 | 1. 新增教练范围消费查询，仅返回当前教练课程相关记录；2. 充值记录默认不开放；3. 隐私字段需 P0 单独确认 |
| DDL 锁表或回填失败 | 生产 `biz_course_booking` 数据量大，直接 ALTER/UPDATE | 下单、签到、支付链路阻塞 | 1. DBA 审核 online DDL；2. 维护窗口或分批迁移；3. 回填按 id 分批并记录断点；4. 应用回退时保留新增字段 |
| 前端头像 404 成破图 | sys_user.avatar 字段为空、或旧数据 URL 过期、或 CDN 故障 | 学员列表全是破图图标，影响专业度 | **三层兜底**：1. Service 层空值替换为常量；2. 工具函数 `getStudentAvatarWithFallback` 兜底；3. `@error` 事件兜底常量；PR6 人工回归 3 种 404 场景 |
| 训练进度"按课程名聚合"被误解为"课程包 1/N" | 教练以为进度条是其购买的 10 节私教课包 | 客诉：显示进度与收费合同不符 | Tab2 页脚强制加**醒目说明卡**（橙色背景 + info 图标）：「进度基于课程名称自动聚合，非正式课程包。如需 1v1 课程包请联系管理员」；为方案 B（正式课程包）保留后续迭代入口 |
| WebSocket 推送风暴 | 批量变更预约状态时一次性推太多事件（如教练批量取消 50 节课） | 教练端 UI 抖动 / 性能下降 / 弹窗轰炸 | 1. 后端事件按 `coachId` 维度做 **200ms 窗口合并**（同一教练的多个事件合并为 1 条）；2. 前端订阅处**防抖 200ms**；3. HIGH 优先级弹窗**1分钟内最多1次**（防止连续取消导致弹窗轰炸）；4. 前端仅刷新当前可见页的局部 state，非可见页延迟到 onShow 刷新 |
| 手机号全号泄露 | 开发者误用 `maskPhone(phone, true)` 在列表页渲染 | 批量泄露学员手机号，违反隐私保护原则 | 1. Code Review 强制检查：`maskPhone(.*?, true)` 只能出现在"拨打确认弹窗"的 `content` 字段；2. 列表页、详情页顶卡**禁止传 `showFull`**；3. 单元测试覆盖 `maskPhone` 的两种模式 |

---

## 9. 下一步建议

> **更新日期：2026-07-14** — P1–P5 功能代码已完成，当前重点为上线验收与 P6 增强。

### 第一批：P0 准入条件

| 检查项 | 负责方 | 验收标准 | 状态 |
|--------|--------|---------|------|
| 《0. 全局设计决策》12 项全部评审通过 | 产品 + 技术 | 会议纪要签字确认 | ⚠️ 文档待签字，**代码已按决策实现** |
| DDL 字段与索引通过 DBA 审核 | DBA | `badminton.sql` 结构复核 + `explain-coach-students.sql` 生产模拟 <500ms | ⚠️ 初始化结构已合并，**DBA 正式审核待确认** |
| 考勤写入链路确认 | 后端开发 + 产品 | 签到/完成/缺席/取消语义与字段写入 | ✅ `updateAttendanceForCoach` 已实现 |
| 消费与账户字段隐私确认 | 产品 + 技术 | 教练仅见本教练课程消费，不返余额/充值 | ✅ 代码已裁剪；**产品签字待确认** |
| 后端配置项补充 | 后端开发 | `coach.risk-threshold.*` | ✅ 已写入 `application.properties` |
| 前端占位图资源 | UI设计 | `/static/placeholders/avatar.svg` | ✅ 已统一使用（P0 冻结覆盖 `avatar-student.svg`） |
| 越权缓存方案 | 架构师 | TTL + 失效时机 | ✅ `coachStudentRelation` 缓存 + `CoachStudentRelationCacheInvalidator` |

### 第二批：P5 收尾（✅ 已完成，2026-07-14）

| # | 任务 | 涉及文件 | 优先级 | 状态 |
|---|------|----------|--------|------|
| 1 | 详情页接入消费记录（独立 Tab） | `student-detail.vue`、`coachSelf.ts` | P0 | ✅ |
| 2 | 课程安排 Tab 拆分「即将上课 / 历史课程」并按双状态分类 | `student-detail.vue`、`coachStudents.ts` | P1 | ✅ |
| 3 | 出勤 Tab 补月度完成/缺席/出勤率汇总 | `student-detail.vue`、`coachStudents.ts` | P1 | ✅ |
| 4 | 概览补近 30 日出勤率 + 近 7 次可点击 mini 时间线 | `student-detail.vue` | P2 | ✅ |
| 5 | `buildCoachStudentListUrl` 支持 `keyword/riskOnly` 预填并由列表页消费 | `student-list.vue`、`coachStudents.ts` | P2 | ✅ |
| 6 | 拨打弹窗统一走 `maskPhone(phone, true)` | `student-detail.vue` | P2 | ✅ |
| 7 | 预约卡片深链由 `bookings.vue` 解析 `id` 并自动打开详情 | `student-detail.vue`、`bookings.vue` | P1 | ✅ |

### 第三批：P6 增强（可灰度后迭代）

**执行顺序**：P5 收尾已合并（`0f454b7`）→ P0 上线验收 → PR7（WS + E2E）

| 任务 | 说明 |
|------|------|
| WebSocket 三类事件 | `COACH_STUDENT_CANCELLED` / `NEW_BOOKING` / `ATTENDANCE_CHANGED` |
| 前端 `coach-ws` 订阅 | 分级响应 + 200ms 防抖 |
| 双账号 E2E 回归 | 教练 + 学员取消/报名/签到全链路 |
| M2 真机 4 case | 有学员 / 空态 / 风险学员 / 到期学员 |
| M3 WS 回归 | 通过后全量上线 |

### 范围外说明

- **Vue Web 管理端教练模块**（`vue/src/views/coach/*`）仍使用 `/api/member/*`，未接入 `/api/coach/students/*`；本计划交付范围为 **BMP-uniapp 教练端**。

### 第三期：可选增强（P0 完成后评估）

**优先级 P1（下个迭代）**：
- 正式课程包模块（方案 B：`biz_course_package` + `biz_member_package` + 扣减流水）
- 监控埋点：学员列表页平均加载时长、越权失败率、头像404率、WebSocket推送延迟

**优先级 P2（按需排期）**：
- 学员 1v1 训练日志 / 评价 / 伤病记录（教练可写，教练端 + 会员端同步可见）
- 教练侧"导出学员档案 PDF / CSV"
- 学员生日提醒 / 到期提醒自动推送

---

## 附录 A：P0 阶段核心决策速查表

| 决策维度 | 最终方案 | 备选方案（为何不选） |
|----------|---------|---------------------|
| 工作台布局 | 3卡片 + 全宽学员卡 | 2×2网格（破坏现有视觉节奏） |
| 越权校验 | EXISTS + Redis缓存 | 每次查库（性能差）、按venueId过滤（不精确） |
| 出勤率算法 | `attendance_status=2` / (`attendance_status=2` + `attendance_status=3`) | 复用预约 `status`（当前无缺席语义）、计入未开始课程（虚高）、仅计入已完成（无法发现缺勤风险） |
| 手机号脱敏 | 列表脱敏 + 弹窗全显 | 全程脱敏（无法拨打）、全程明文（泄露风险） |
| 头像兜底 | Service + 工具函数 + onerror 三层 | 仅前端兜底（后端返回null崩溃） |
| 训练进度方案 | 按课程名聚合（方案A） | 正式课程包（方案B，改造量大） |
| WebSocket优先级 | HIGH/MEDIUM/LOW分级 | 平等推送（通知疲劳） |

---

## 附录 B：计划 vs 实际落地对照（2026-07-13）

### B.1 架构调整（合理演进，不影响功能）

| 维度 | 原计划 | 实际落地 | 评价 |
|------|--------|----------|------|
| 聚合 SQL 位置 | `CourseBookingMapper` 新增 7 方法 | 独立 `CoachStudentMapper` | ✅ 职责更清晰 |
| REST 入口 | 修改 `CoachController` | 独立 `CoachStudentController` | ✅ 避免 Controller 膨胀 |
| 前端工具文件 | `utils/coachView.ts` | `utils/coachStudents.ts` | ✅ 命名更贴切 |
| TS 类型位置 | `coachSelf.ts` 底部 | `types/coachStudents.ts` | ✅ 可维护性更好 |
| 关系缓存 | `@Cacheable` 注解 | 手动 `CacheManager` | ✅ 效果等价 |
| 详情页路由参数 | `memberId` | `id` | ⚠️ 文档已同步 |
| 学员占位图 | `avatar-student.svg` | `avatar.svg`（P0 冻结覆盖） | ✅ 已对齐 |

### B.2 核心文件速查

**后端：**
- `src/main/java/com/badminton/bmp/modules/coach/controller/CoachStudentController.java`
- `src/main/java/com/badminton/bmp/modules/coach/service/impl/CoachStudentServiceImpl.java`
- `src/main/java/com/badminton/bmp/modules/coach/mapper/CoachStudentMapper.java`
- `src/main/resources/sql/badminton.sql`
- `src/main/resources/sql/explain-coach-students.sql`

**前端（BMP-uniapp）：**
- `BMP-uniapp/pages/coach/student-list.vue`
- `BMP-uniapp/pages/coach/student-detail.vue`
- `BMP-uniapp/api/coachSelf.ts`
- `BMP-uniapp/types/coachStudents.ts`
- `BMP-uniapp/utils/coachStudents.ts`

**测试：**
- `src/test/java/com/badminton/bmp/modules/coach/CoachStudent*.java`（实际 5 个类）
- `src/test/java/com/badminton/bmp/modules/course/**/CourseBooking*AttendanceTest.java`（实际 2 个类）
- `BMP-uniapp/tests/coachStudentsApi.spec.ts`
- `BMP-uniapp/tests/coachStudentsUi.spec.ts`

---

**文档结束。** 涂夫哥 · 进度更新 2026-07-14
